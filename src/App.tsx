import { ArchitecturePage } from '@/pages/ArchitecturePage';

// Phase 1 builds a single route: the Total Architecture at /architecture.
// A full router is out of scope until later phases add more routes.
export default function App() {
  return <ArchitecturePage />;
}
